icondir = join_paths('icons', 'hicolor', '24x24', 'apps')

install_data(
  'desktop-icon.png',
  install_dir: join_paths(datadir, icondir),
  rename: '@0@.png'.format(meson.project_name())
)

install_data(
  'apricots.desktop',
  install_dir: desktopdir
)
